const plantNeedsWater = function(day) {
  if (day === 'Wednesday'){
    return true;
 } else {
   return false;
 }
}
/* I learned to use function expressions to create an anonymous function */
console.log(plantNeedsWater('Tuesday'));
/* I used function expressions to define a function */
