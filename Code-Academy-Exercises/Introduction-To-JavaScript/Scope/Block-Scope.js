const logVisibleLightWaves = () => {
  const lightWaves='Moonlight';
  console.log(lightWaves);
};
logVisibleLightWaves();
console.log(lightWaves);
/* I used local variables within the block scope in a function, demonstrating that referrring to a block scope variable outside of its block will produce a "not defined" error */
